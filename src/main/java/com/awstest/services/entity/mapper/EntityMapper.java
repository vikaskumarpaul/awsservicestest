package com.awstest.services.entity.mapper;

/*import com.test.awsservices.entity.DeviceServiceAssociationEntity;
import com.test.awsservices.model.DeviceServiceAssociation;
import com.test.awsservices.model.RessourceMetadata;*/

public class EntityMapper
{

    /*	*//**
           * Util mapper to map DeviceServiceAssociationEntity to DeviceServiceAssociation
           * 
           * @param deviceServiceEntity
           * @return {@link DeviceServiceAssociation}
           *//*
              * public static DeviceServiceAssociation
              * DSAEntityToDSA(DeviceServiceAssociationEntity deviceServiceEntity) { if
              * (deviceServiceEntity != null) {
              * 
              * DeviceServiceAssociation dsa = new DeviceServiceAssociation();
              * RessourceMetadata metadata = new RessourceMetadata();
              * dsa.setId(deviceServiceEntity.getId()); // TODO: there is no field with this
              * name in table device Service Association // and will be changed in future as
              * dsa name i.e. yaml need to update
              * dsa.setSeviceGroupName(deviceServiceEntity.getDsaName());
              * metadata.setCreatedTs(deviceServiceEntity.getCreatedAt());
              * metadata.setCreatedBy(deviceServiceEntity.getCreatedBy());
              * metadata.setLastModifiedBy(deviceServiceEntity.getLastUpdatedBy());
              * metadata.setLastModifiedTs(deviceServiceEntity.getLastUpdatedAt());
              * metadata.setVersion(deviceServiceEntity.getVersion().intValue());
              * dsa.setRessourceMetadata(metadata);
              * 
              * return dsa; } else return new DeviceServiceAssociation(); }
              */
}
